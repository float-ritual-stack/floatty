interface:
  display_name: "Floatty"
  short_description: "Repo-specific patterns and workflows for floatty"
  default_prompt: "Use the floatty repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true